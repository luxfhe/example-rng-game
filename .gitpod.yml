image: "gitpod/workspace-node-lts:latest"

tasks:
  - init: "pnpm install && cp .env.example .env && pnpm hardhat localluxfhe:start"

vscode:
  extensions:
    - "esbenp.prettier-vscode"
    - "NomicFoundation.hardhat-solidity"
    - "ritwickdey.LiveServer"
